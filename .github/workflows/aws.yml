# Existing content here...

- name: Clear Gradle cache
  run: |
    rm -rf ~/.gradle/
    rm -rf build/
    rm -rf .gradle/
    ls -la gradle/common/

- name: Build with Gradle
  run: ./gradlew clean build

# Rest of the workflow steps here...